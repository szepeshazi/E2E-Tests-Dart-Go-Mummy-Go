//    driver.window.catchError((onError) => print(onError));

//var options = {
//  'browserName': 'chrome'
//    ,'chromeOptions': {
//      'binary': 'C:\\Program Files (x86)\\Google\\Chrome\\Application\\chrome.exe',
//      'args': ['start-maximized', '--enable-logging', '--error-console', '--log-level=2']
//    }
//};

//var loginBtn = await driver
//    .findElement(
//const By.cssSelector('div.menu-item.menu-item--login > a'));
//await loginBtn.click();

//driver.logs.get('browser').where((LogEntry l) =>
//l.level == 'SEVERE' ).listen((LogEntry l) => print(l));

//    var overlay = await driver.findElement(const By.cssSelector(
//        "body > my-app > profile-router > product-module > cutout-shadow-overlay > div.overlay"));
//    await waitForAngular(driver);
//    expect(await elementExists(overlay), true);


// Add the following class definition to main.dart.

//
//group("Trial Period Tests |", () {
  //setUp(() async {
    //await driver.get(url);
    //await loginUserWithCredentials(
    //newUserEmail, allUserPassword, driver, true);
    //await waitForAngular(driver);
  //});
  //
  //test('Invoicing Page shows User has active subscription', () async {
    //await navigateToPage(driver, 'profile/settings/profile');
    //await waitForAngular(driver);
    //await sleep2();
    //var invoicingTabBtn = await driver
    //    .findElement(const By.xpath("*//tab-button[3]/material-ripple"));
    //await invoicingTabBtn.click();
    //var noActiveSubsHeader = await driver.findElement(const By.xpath(
    //"*//active-subscriptions/*//h1[contains(.,'Nincs aktív előfizetés')]"));
    //expect(elementExists(noActiveSubsHeader), true);
  //});
//});

